# Core dependencies for Local Whisper GUI
# AI/ML Libraries
# Note: torch and torchaudio must be installed separately before this file.
#   CPU:  pip install torch torchaudio --index-url https://download.pytorch.org/whl/cpu
#   CUDA: pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu124
openai-whisper==20250625

# User Interface
gradio==6.8

# Audio Processing
librosa==0.11.0
soundfile==0.13.1
scipy==1.16.3

# Data Processing
numpy==2.4.2
pandas==3.0.1

# Utilities
tqdm==4.67.3
python-dotenv==1.2.2

# Note: pyaudio is not included due to compilation issues on some systems.
# The application uses Gradio's built-in audio handling instead.